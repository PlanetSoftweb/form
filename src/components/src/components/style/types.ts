export interface FormStyle {
  backgroundColor: string;
  backgroundGradient?: {
    enabled: boolean;
    startColor: string;
    endColor: string;
    direction: string;
  };
  textColor: string;
  buttonColor: string;
  borderRadius: string;
  borderWidth: string;
  borderColor: string;
  borderStyle: string;
  boxShadow: string;
  fontFamily: string;
}